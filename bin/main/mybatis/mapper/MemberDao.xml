<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- SQL 끝에 ; 세미콜론을 넣지 마세요! -->

<mapper namespace="com.study.springboot.dao.IMemberDao">

	<select id="login" resultType="_int">
		SELECT COUNT(*) FROM genies_member WHERE member_id = #{param1} AND member_pw = #{param2}
	</select>     
	
	<insert id="join">
		INSERT INTO genies_member( member_idx, member_id, member_pw, member_name, member_email,
		member_email_receive, member_pw_question, member_pw_answer, member_phone, member_address, member_join_date )
		VALUES ( genies_member_seq.nextval, #{param1}, #{param2}, #{param3}, #{param4}, #{param5},
		#{param6}, #{param7}, #{param8}, #{param9}, sysdate )
	</insert>
	
	<select id="viewMember" resultType="com.study.springboot.dto.MemberDto">
    	select * from GENIES_MEMBER where MEMBER_ID=#{ param1 }
    </select>
	
	<update id="memberUpdate">
		UPDATE GENIES_MEMBER SET 
		MEMBER_ID=#{param1}, 
		MEMBER_NAME=#{param2},
		MEMBER_PHONE=#{param3},
		MEMBER_EMAIL=#{param4}
		WHERE MEMBER_ID = #{param5}
	</update>
		
</mapper>    


